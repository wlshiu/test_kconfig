1. add the path of Qt include 
2. add the path of Qt lib (*.lib)
3. change "ALL" project to release mode
4. set "ALL" project to static_link (Property -> C/C++ -> Code Generation -> Runtime library -> /MT)

